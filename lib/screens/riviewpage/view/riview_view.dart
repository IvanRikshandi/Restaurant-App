import 'package:flutter/material.dart';

class RiviewView extends StatelessWidget {
  const RiviewView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
